# Experiments

Team repository for our RAG project. Each area is owned by a different teammate:

- **Knowledge base** – building and maintaining the knowledge base
- **System prompt** – designing the system prompt
- **Guardrails** – input/output guardrails
- **Retrieval** – retrieval pipeline

Push your work to `main` (or use a feature branch and open a pull request).
